import React from 'react';
import GuestLayout from './guest-layout';

export default () => (
    <GuestLayout>
        <header className="postHeader">
            <h1>Todo</h1>
        </header>
    </GuestLayout>
);